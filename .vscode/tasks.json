{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: build (preset)",
            "type": "shell",
            "command": "cmake --build --preset wsl-arm64-debug",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}